{% extends 'base.html.twig' %}

{% block body %}

  <h1 class='text-primary' style="margin-left:25%; margin-top:90px;"> Categorie</h1>

  <div class="jumbotron border border-primary rounded shadow-lg w-50 fixed" style="margin-left:25%;margin-top:-10px">
    <div class="container text-center">
    {% for question in questions %}
    {% for categorie in categories %}
    <div>
    <h5>
    <buttton type="button" class='btn btn-primary rounded-pill shadow'> <a class="text-light" href="{{ path('show', {idcategorie: categorie.id, id: question.id})}}">{{ categorie.name}}</a> </buttton>
    </h5>
    </div>
    {% endfor %}
    {% endfor %}

    </div>
  </div>
{% endblock %}
